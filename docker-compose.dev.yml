version: '3.8'

services:
  firestore-emulator:
    image: gcr.io/google.com/cloudsdktool/google-cloud-cli:alpine
    container_name: neighbour-approved-firestore
    ports:
      - "8080:8080"  # Firestore emulator
    environment:
      - FIRESTORE_PROJECT_ID=neighbour-approved-dev
      - GOOGLE_CLOUD_PROJECT=neighbour-approved-dev
    command: >
      sh -c "
        apk add --no-cache curl openjdk21-jre &&
        gcloud components install beta cloud-firestore-emulator --quiet &&
        gcloud config set project neighbour-approved-dev &&
        gcloud beta emulators firestore start --host-port=0.0.0.0:8080 --project=neighbour-approved-dev
      "
    volumes:
      - firestore-data:/opt/data
    networks:
      - neighbour-approved-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

volumes:
  firestore-data:
    driver: local

networks:
  neighbour-approved-network:
    driver: bridge